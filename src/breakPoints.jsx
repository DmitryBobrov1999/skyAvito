const size = {
	min: '590px',
	minAverage: '768px',
	average: '890px',
	max: '1158px',
};

export const device = {
	min: `(max-width: ${size.min})`,
	minAverage: `(max-width: ${size.minAverage})`,
	average: `(max-width: ${size.average})`,
	max: `(max-width: ${size.max})`,
	
};
